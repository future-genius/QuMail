# QuMail Configuration
# Copy this file to .env and fill in your email credentials

# SMTP Configuration (for sending emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
SENDER_EMAIL=your_email@gmail.com

# IMAP Configuration (for receiving emails)
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USER=your_email@gmail.com
IMAP_PASS=your_app_password

# Gmail Setup Instructions:
# 1. Enable 2-factor authentication on your Google account
# 2. Go to Google Account settings > Security > 2-Step Verification
# 3. Generate an App Password for QuMail
# 4. Use the generated App Password in SMTP_PASS and IMAP_PASS above
# 5. Make sure IMAP is enabled in Gmail settings

# Outlook Setup Instructions:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# IMAP_HOST=outlook.office365.com
# IMAP_PORT=993
# Use your regular Outlook password or App Password